# Build outputs and caches — never commit binaries or weights
dist/
.cache/
node_modules/
__pycache__/
*.gguf
*.log
.DS_Store
._*

# Scratch chats/docs from running pocketd locally against the repo copy of ui/
.testdata/
ui/machine.json
