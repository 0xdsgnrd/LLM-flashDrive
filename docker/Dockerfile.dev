FROM node:22-bookworm-slim
RUN apt-get update && apt-get install -y --no-install-recommends \
      python3 python3-pip python3-venv curl ca-certificates git \
 && rm -rf /var/lib/apt/lists/*
WORKDIR /work
EXPOSE 5173
CMD ["node", "scripts/devserver.mjs"]
